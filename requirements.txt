# Compatible versions that resolve dependency conflicts
fastapi>=0.108.0
uvicorn>=0.25.0
pydantic>=2.6.0
httpx>=0.26.0
python-multipart>=0.0.6

# Note: Removed mcp==1.0.0 as it conflicts with FastAPI's anyio requirements
# This SSE MCP server implements the MCP protocol directly without the mcp package

# Production deployment compatible versions
anyio>=3.7.1,<4.0.0

# Development dependencies (install with: pip install -r requirements.txt -r test_requirements.txt)
# Or install testing dependencies separately: pip install -r test_requirements.txt

